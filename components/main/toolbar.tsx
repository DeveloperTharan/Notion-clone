'use client'

import React from 'react'
import { Document } from '@prisma/client'

export const ToolBar = ({initialData}: { initialData: Document | null}) => {
  return (
    <div>ToolBar</div>
  )
}
